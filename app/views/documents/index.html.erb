<h1>提出された書類の有効期限一覧</h1>
<h2>免許証</h2>
<% if @driver_licenses.empty? %>
  <p>提出されている免許証はありません。</p>
<% else %>
  <% @driver_licenses.each do |license| %>
    <p>有効期限: <%= license.expiration_date %></p>
    <p><%= link_to '編集する', edit_driver_license_path(license), class: 'btn btn-secondary' %></p>
  <% end %>
<% end %>
<h2>車検証</h2>
<% if @vehicle_inspections.empty? %>
  <p>提出されている車検証はありません。</p>
<% else %>
  <% @vehicle_inspections.each do |inspection| %>
    <p>有効期限: <%= inspection.expiration_date %></p>
    <p><%= link_to '編集する', edit_vehicle_inspection_path(inspection), class: 'btn btn-secondary' %></p>
  <% end %>
<% end %>
<h2>自賠責保険</h2>
<% if @compulsory_insurances.empty? %>
  <p>提出されている自賠責保険はありません。</p>
<% else %>
  <% @compulsory_insurances.each do |insurance| %>
    <p>有効期限: <%= insurance.expiration_date %></p>
    <p><%= link_to '編集する', edit_compulsory_insurance_path(insurance), class: 'btn btn-secondary' %></p>
  <% end %>
<% end %>
<h2>任意保険</h2>
<% if @optional_insurances.empty? %>
  <p>提出されている任意保険はありません。</p>
<% else %>
  <% @optional_insurances.each do |insurance| %>
    <p>有効期限: <%= insurance.expiration_date %></p>
    <p><%= link_to '編集する', edit_optional_insurance_path(insurance), class: 'btn btn-secondary' %></p>
  <% end %>
<% end %>
<% if @driver_licenses.empty? && @vehicle_inspections.empty? && @compulsory_insurances.empty? && @optional_insurances.empty? %>
  <div style="margin-top: 20px;">
    <%= link_to '新規提出', new_driver_license_path, class: 'btn btn-primary' %>
  </div>
<% end %>
